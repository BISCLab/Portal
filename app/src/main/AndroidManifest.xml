<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".PortalApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Portal"
        android:allowBackup="false">

        <!-- Main launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tiny Portal widget configuration activity -->
        <activity
            android:name=".ui.widget.TinyPortalConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.Portal">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Tiny Portal widget provider -->
        <receiver
            android:name=".ui.widget.TinyPortalWidget"
            android:exported="true"
            android:label="@string/widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/tiny_portal_widget_info" />
        </receiver>

        <!-- Row Portal widget configuration activity -->
        <activity
            android:name=".ui.widget.RowPortalConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.Portal">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Row Portal widget provider -->
        <receiver
            android:name=".ui.widget.RowPortalWidget"
            android:exported="true"
            android:label="@string/row_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/row_portal_widget_info" />
        </receiver>

    </application>

</manifest>
